<%= form_for(@voucher_account) do |f| %>
  <% if @voucher_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@voucher_account.errors.count, "error") %> prohibited this voucher account from being saved:</h2>

      <ul>
      <% @voucher_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :no %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
